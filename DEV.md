# 修仙洞天 · 项目状态文档

> 最后更新：2026-05-13 16:11

## 📌 项目概览

**修仙洞天**（暂定名）是一款文字类修仙题材冒险养成游戏，参考《辐射避难所》的核心养成玩法，融合修仙世界观。

- **类型**：文字养成 + 基地管理 + 探索冒险
- **平台**：Web（浏览器）
- **灵感来源**：辐射避难所（基地建设、人物管理、资源产出、探索派遣）

---

## 🛠 技术栈

| 层面 | 技术 | 版本 |
|------|------|------|
| 框架 | Vite + React | 8.0.12 / 19.x |
| 语言 | TypeScript | 5.x |
| 状态管理 | Zustand (persist) | 5.x |
| 样式 | TailwindCSS v4 | 4.x |
| 动画 | Framer Motion | 12.x |
| 持久化 | localStorage | — |

**路径**：`G:\openclaw\xiuxian-haven`  
**开发端口**：`http://localhost:5173`

---

## 📁 项目结构

```
xiuxian-haven/
├── src/
│   ├── data/
│   │   ├── types.ts          # 全部游戏类型定义
│   │   └── constants.ts      # 游戏常量、模板、工具函数
│   ├── store/
│   │   └── gameStore.ts      # Zustand 全局状态 + 游戏主循环
│   ├── components/
│   │   ├── GameLayout.tsx     # 主布局（标签切换 + 动画）
│   │   ├── TopBar.tsx         # 顶部状态栏（资源、速度控制）
│   │   └── panels/
│   │       ├── HavenPanel.tsx      # 洞府面板（建筑管理）
│   │       ├── DisciplesPanel.tsx  # 弟子面板（列表+详情）
│   │       ├── ExplorePanel.tsx    # 探索面板（任务派遣）
│   │       └── MessagesPanel.tsx   # 消息面板（事件日志）
│   ├── App.tsx
│   ├── main.tsx
│   └── index.css             # TailwindCSS + 自定义主题 + 动画
├── vite.config.ts
├── tsconfig.json
└── package.json
```

---

## ✅ 已完成

### 核心系统

| 模块 | 状态 | 说明 |
|------|------|------|
| 类型系统 | ✅ | 完整的 TypeScript 类型定义（弟子、建筑、资源、任务、法器、丹药等） |
| 游戏状态 | ✅ | Zustand store + localStorage 持久化，自动存档 |
| 游戏主循环 | ✅ | 1秒 tick，资源产出、修炼进度、探索完成检查、buff过期处理 |
| 游戏速度 | ✅ | 支持 1x / 2x / 5x 加速 |

### 弟子系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 随机生成 | ✅ | 姓名、性别、灵根、特质、属性全部随机 |
| 灵根系统 | ✅ | 金木水火土，天灵根/双灵根/三灵根/杂灵根 |
| 特质系统 | ✅ | 10种特质，影响修炼/战斗/探索 |
| 境界体系 | ✅ | 练气→筑基→金丹→元婴→化神→渡劫→大乘→真仙 |
| 修炼突破 | ✅ | 自动修炼 + 概率突破 + 失败回退 |
| 属性展示 | ✅ | 悟性/魅力/气运/心志/感知 + 进度条 |
| 招募 | ✅ | 消耗100灵石，灵石不足无法招募 |
| 逐出 | ✅ | 确认后逐出空闲弟子，自动清理建筑分配 |

### 建筑系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 建筑类型 | ✅ | 12种：修炼室、丹房、炼器房、聚灵阵、藏经阁、灵田、灵矿、会客厅、寝殿、练功场、秘境入口、护山大阵 |
| 建造 | ✅ | 消耗灵石/材料建造 |
| 升级 | ✅ | 多级升级，消耗递增 |
| 弟子分配 | ✅ | 分配弟子到建筑，影响产出效率 |
| 资源产出 | ✅ | 根据建筑等级和弟子效率自动产出 |

### 探索系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 任务模板 | ✅ | 5种：探秘、讨伐、采集、历练、秘境 |
| 派遣 | ✅ | 选择空闲弟子出发 |
| 进度追踪 | ✅ | 基于游戏时间的实时进度条 |
| 奖励发放 | ✅ | 完成后自动获得灵石、修为 |

### 丹药系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 丹药种类 | ✅ | 6种：回春丹、聚气丹、凝神丹、运灵丹、破境丹、九转还魂丹 |
| 使用效果 | ✅ | 回复生命、增加修为、属性buff、提升突破概率 |
| Buff系统 | ✅ | 限时增益效果，自动过期提示 |
| UI | ✅ | 弟子详情中可展开丹药列表使用 |

### UI 系统

| 功能 | 状态 | 说明 |
|------|------|------|
| 主布局 | ✅ | 3标签切换（洞府/弟子/探索）+ 动画过渡 |
| 顶部状态栏 | ✅ | 灵石、灵气、弟子数、最强弟子、速度控制、保存 |
| 消息日志 | ✅ | 左下角常驻消息流，支持展开/收起 |
| 水墨风格 | ✅ | 深色主题 + 金色点缀 + 卷轴纹理 |
| 响应式 | ✅ | 移动端适配 |

---

## 🚧 未完成 / 待开发

### 高优先级

| 功能 | 说明 |
|------|------|
| **探索随机事件** | 探索过程中触发选择事件，8种事件 + 属性检定 + 气运加成 + 弹窗UI | ✅ |
| **法器系统** | 装备给弟子、品质分级、属性加成 | 🚧 |
| **战斗系统** | 弟子间战斗 / 妖兽战斗的回合制文字战斗 | ✅ |
| **丹房炼丹UI** | 用材料炼制丹药，目前丹药只在初始赠送 | ✅ |

### 中优先级

| 功能 | 说明 |
|------|------|
| **更多探索任务** | 动态任务生成、难度递进、剧情线 | ✅ |
| **弟子互动** | 弟子间关系、切磋、传功 | ✅ |
| **访客系统** | 会客厅接待访客，交易/招募/事件 | ✅ |
| **成就系统** | 达成条件解锁成就 | ✅ |
| **新手引导** | 教程流程 | ❌（由主线剧情替代） |

### 低优先级

| 功能 | 说明 |
|------|------|
| **音效/BGM** | 背景音乐、操作音效 |
| **存档管理** | 多存档、导入导出 |
| **离线收益** | 关闭浏览器后回来领取离线期间收益 |
| **随机事件池** | 洞府内突发事件（妖兽入侵、天降机缘等） |
| **排行榜** | 纯本地的成就展示 |

---

## 🐛 Bug 修复记录

### 2026-05-13

1. **游戏速度双重计算** — tick() 中 `tickMs` 已含 gameSpeed，修炼速度又乘了一次，导致 2x 实际 4x。修复：移除修炼速度中的 gameSpeed 乘数。
2. **容量为 0 建筑 NaN** — 聚灵阵等 capacity=0 建筑，效率计算 `length/0 = NaN`。修复：跳过 capacity<=0 的建筑。
3. **探索任务用真实时间** — 任务完成用 Date.now() 而非游戏时间，与游戏速度不一致。修复：改为基于 gameTime。
4. **探索任务模板重复** — startExploration 和 ExplorePanel 各自定义模板。修复：重构为调用方传入任务数据。
5. **招募返回类型不安全** — 灵石不足时 `null as unknown as Disciple`。修复：接口改为 `Disciple | null`。
6. **灵气回复 gameSpeed 双重计算** — `ENERGY_REGEN_RATE * gameSpeed * (tickMs/1000)` 中 tickMs 已含 gameSpeed。修复：移除多余的 gameSpeed 乘数。
7. **建筑修炼速度加成未生效** — 修炼室/藏经阁/练功场的 `cultivationSpeed` 产出在修炼计算中完全未使用。修复：tick() 中查找弟子所在建筑的 cultivationSpeed 并作为加成系数。
8. **真仙进度条 NaN%** — `requiredQi=0` 导致除以零。修复：判断 `requiredQi > 0` 再计算百分比，否则显示 100%。
9. **升级未检查材料** — `upgradeRoom` 只检查灵石，材料不足时仍扣灵石。修复：增加材料检查和扣除逻辑。
10. **探索奖励硬编码** — `startExploration` 固定给 50 灵石+20 修为，与 UI 不一致。修复：接口增加 rewards 参数，ExplorePanel 传入具体奖励。
11. **藏经阁/练功场弟子不修炼** — 分配到这些建筑的弟子状态为 idle，不触发修炼。修复：分配到有 cultivationSpeed 的建筑时也设为 cultivating。
12. **法器加成未算入战斗** — resolveCombat 只用基础 attack/defense，法器属性被忽略。修复：函数增加 artifactBonus 参数，tick() 中计算后传入。
13. **探索完成事件残留** — 任务完成后 activeEvents 未清理，导致已结束任务的事件弹窗残留。修复：任务完成时清理关联 activeEvents。
14. **寝殿无HP回复** — 弟子受伤后无法恢复。修复：寝殿弟子每 tick 回复 2+level HP。
15. **随机事件set循环覆盖** — 循环内逐个 set()，多事件同 tick 时后者覆盖前者。修复：批量收集后一次 set。
16. **法器类型断言不安全** — `as typeof ARTIFACT_TEMPLATES` 模板无 id 字段。修复：改为显式 stats 类型断言。
17. **战斗属性显示不一致** — 显示 base+bonus 但战斗只用 base。修复：显示合计值（base+bonus）。
18. **建造材料扣减竞态** — 两次 set()，第一次未实际扣材料。修复：合并为单次 set()。
19. **事件概率5x速度膨胀** — `probability * (tickMs/60000)` 在 5x 时概率 5 倍。修复：上限 15%/tick。

---

## 🎮 游戏设计参考（辐射避难所 → 修仙洞天）

| 辐射避难所 | 修仙洞天 | 状态 |
|-----------|---------|------|
| Vault（避难所） | 洞府 | ✅ |
| Dweller（居民） | 弟子 | ✅ |
| S.P.E.C.I.A.L. 属性 | 悟性/魅力/气运/心志/感知 | ✅ |
| Room（房间） | 建筑 | ✅ |
| Caps（瓶盖） | 灵石 | ✅ |
| Water/Food | 灵气/材料 | ✅ |
| Wasteland Quest | 探索任务 | ✅ |
| Random Events | 探索随机事件 | ✅ |
| Crafting (Weapons) | 炼器 | ✅ |
| Crafting (Stimpaks) | 炼丹 | ✅ |
| Deathclaw Attack | 战斗系统 | ✅ |
| Happiness | 弟子满意度 | ❌ |

---

## 🔧 开发命令

```bash
cd G:\openclaw\xiuxian-haven

# 开发
npm run dev          # 启动开发服务器 (localhost:5173)

# 构建
npm run build        # TypeScript 检查 + Vite 生产构建

# 代码检查
npx tsc --noEmit     # 仅 TypeScript 类型检查
```

---

## 💾 存档机制

- 使用 Zustand `persist` 中间件自动存储到 `localStorage`
- 存档 key：`xiuxian-haven-save`
- 自动保存间隔：30秒（可配置）
- 存档内容：洞府、弟子、资源、消息、游戏时间、设置

---

## 📏 开发规范

### 变更记录（强制）

**每次更新、改动、修复都必须记录在本文档中。** 包括但不限于：
- 代码改动（新功能、重构、优化）
- Bug 修复
- 配置变更
- 依赖更新
- 设计决策

记录格式：在对应章节追加条目，并在底部「Bug 修复记录」或新增「变更日志」章节记录详情。

---

## 📝 开发备忘

### 代码风格
- 函数组件 + Hooks
- 状态全部在 Zustand store，组件只读取和调用 action
- TailwindCSS utility-first，自定义颜色变量在 `index.css` 的 `@theme` 中
- 动画用 Framer Motion，简单过渡用 CSS

### 注意事项
- `gameStore.ts` 中 `tick()` 函数是游戏核心循环，修改需谨慎
- 境界突破逻辑在 `tick()` 中，涉及修为进度和概率
- 建筑产出效率 = `弟子数/容量 × (1 + 0.2 × (等级-1))`
- 所有 ID 用 `generateId()`（时间戳 + 随机串）

### 已知技术债
- `DisciplesPanel.tsx` 中 `DiscipleDetail` 组件较复杂，后续可拆分
- 缺少错误边界（Error Boundary）

---

## 📋 变更日志

### 2026-05-13

| 时间 | 类型 | 内容 |
|------|------|------|
| 00:21 | UI | 消息窗口改为左下角常驻消息流，支持展开/收起/暂停滚动 |
| 00:26 | Bug | 修复招募弟子未扣除灵石 |
| 00:30 | 功能 | 添加逐出弟子功能（含二次确认） |
| 00:41 | Bug | 修复游戏速度双重计算、容量NaN、探索时间、模板重复、返回类型 |
| 00:41 | 功能 | 新增丹药系统：6种丹药 + buff效果 + UI |
| 00:55 | 规范 | 建立变更记录强制规范 |
| 11:13 | Bug | 修复灵气回复双重计算、建筑修炼加成未生效、真仙进度条NaN、升级未检查材料、探索奖励硬编码、藏经阁/练功场不修炼（共6个） |
| 11:13 | 功能 | 新增探索随机事件系统：8种事件 + 属性检定 + 气运加成 + 弹窗UI |
| 11:13 | 功能 | 新增法器装备系统：装备/卸下（最多3件）、属性加成显示、探索掉落 |
| 11:13 | 功能 | 新增战斗系统：讨伐任务自动战斗、回合制文字战斗、HP消耗、战斗日志 |
| 12:37 | Bug | 修复法器加成未算入战斗、探索完成事件残留、寝殿无HP回复 |
| 12:40 | Bug | 修复随机事件set循环覆盖、法器类型断言、战斗属性显示不一致、建造材料扣减竞态、事件概率5x膨胀 |
| 12:56 | UI | 全面UI优化：弟子卡片HP条+状态指示灯、顶栏材料+游戏时间、建造菜单显示材料消耗、房间卡片产出摘要、探索推荐战力、消息时间戳、渐变进度条、卡片悬浮效果、全局动画增强 |
| 14:14 | 功能 | 新增炼丹系统：6种丹药配方、材料消耗、丹房弟子悟性影响成功率、建筑等级加成 |
| 14:14 | 功能 | 新增锻造系统：8种法器配方、材料消耗、炼器房弟子感知影响成功率、建筑等级加成 |
| 14:14 | UI | 丹房/炼器房展开显示炼制配方列表，点击展开详情可炼制 |
| 15:09 | Bug | 修复最高级突破死循环、usePill/craftPill/forgeArtifact多次set竞态（共5个） |
| 15:09 | 功能 | 新增动态探索任务：8种任务模板，难度随游戏时间和洞府等级递进 |
| 15:09 | 功能 | 新增访客系统：会客厅有弟子时随机来客（5种访客），可交易/招募/获赠 |
| 15:09 | 功能 | 新增弟子互动：传功（30%修为转移）、切磋（双方获经验） |
| 15:09 | 功能 | 新增成就系统：8个成就，自动解锁，弹窗提示 |
| 15:30 | UI | 消息栏改为三态切换（收起/展开/隐藏），隐藏后仅显示📜小图标 |
| 15:52 | Bug | 修复旧存档缺失 achievements/activeVisitor 字段导致崩溃，全面加 ?? [] 防御 |
| 16:11 | 功能 | 新增主线剧情系统：5章15个任务，完整叙事文本，进度追踪，自动解锁 |
| 16:30 | Bug | 修复主线奖励多次set竞态、探索计数误匹配、境界检查缺安全、成就条件不准确（共4个） |
| 16:48 | 功能 | 主线任务奖励改为手动点击领取，完成目标后显示🎁领取按钮 |
| 20:05 | 功能 | 新增音效/BGM系统：Web Audio API 合成 15 种音效 + 古风旋律 BGM + 设置面板音量控制 |

### 2026-05-12

| 时间 | 类型 | 内容 |
|------|------|------|
| 17:43 | 项目 | 初始化项目骨架（Vite + React + TS + Zustand + TailwindCSS） |
| 17:43 | 功能 | 核心游戏系统：弟子、建筑、探索、资源、修炼 |
